{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Jwt": {
    "Issuer": "IdentityService",
    "Audience": "AssessmentPortal",
    "ExpiryMinutes": "60"
  },
  "AllowedOrigins": [
    "http://localhost:4200",
    "http://localhost:55942",
    "http://frontend",
    "http://frontend:80"
  ],
  "AllowedHosts": "*",
  "Notes": {
    "Secrets": "JWT:Key and ConnectionStrings are loaded from User Secrets (dev) or Environment Variables (prod)",
    "DevSetup": "Run: dotnet user-secrets init && dotnet user-secrets set \"Jwt:Key\" \"your-256-bit-key\" && dotnet user-secrets set \"ConnectionStrings:DefaultConnection\" \"your-connection-string\""
  }
}


